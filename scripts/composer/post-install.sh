#!/bin/sh

# dir=$(cd -P -- "$(dirname -- "$0")" && pwd -P)
#
# cd $dir/../..
#
# wp theme activate
#
# wp rewrite flush

# cp -rp www/wp-content/themes/_s www/wp-content/themes/wordpress-starter-kit
# rsync -av --progress wp-content/themes/_s wp-content/themes/wordpress-starter-kit --exclude .git
